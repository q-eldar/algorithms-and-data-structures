#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
using namespace std;

int lis(vector<int> seq, int till, int index) {
  int res = 1;
  for (int i = index; i < seq.size(); i++) {
    if (seq[i] < )
  }
}

int main() {
  vector<int> seq{10, 9, 3, 5, 4, 11, 7, 8};
  cout << lis(seq, 1) << endl;
}
